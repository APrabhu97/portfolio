import * as React from "react";
import Loader from "../components/loader/loader";

// markup
const IndexPage = () => {
  return (
    <div>
      <Loader></Loader>
    </div>
  );
};

export default IndexPage;
